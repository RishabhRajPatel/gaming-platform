name: Teen Patti APK

# Builds an installable Android APK in CI (where Google's Android repos are reachable).
# Download it from the run's "Artifacts" section. Trigger manually or on push to the app.

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "teen-patti/**"
      - ".github/workflows/teen-patti-apk.yml"

jobs:
  build-apk:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: teen-patti
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: teen-patti/package-lock.json

      - name: Install Capacitor deps
        run: npm ci || npm install

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Build debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: teen-patti-debug-apk
          path: teen-patti/android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
